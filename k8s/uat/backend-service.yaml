apiVersion: v1
kind: Service
metadata:
  name: tracking-debug-backend-master
  namespace: atlas
spec:
  type: ClusterIP
  selector:
    app: tracking-debug-backend-master
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
