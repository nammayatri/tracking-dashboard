apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: atlas

resources:
  - secrets.yaml
  - backend-deployment.yaml
  - backend-service.yaml
  - frontend-minimal-deployment.yaml
  - frontend-minimal-service.yaml
  # Full frontend not deployed in UAT for now; uncomment to enable.
  # - frontend-deployment.yaml
  # - frontend-service.yaml
